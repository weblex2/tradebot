# Session 2026-03-21b

## Änderungen

### Crypto Asset Icons
- Neuer Blade-Component `resources/views/components/asset-icon.blade.php`
- Icons via CDN: `cdn.jsdelivr.net/gh/spothq/cryptocurrency-icons` (alle 26 Assets abgedeckt)
- Eingebaut in: Dashboard (Decisions-Tabelle, Signals-Tabelle, Coinbase Portfolio), Trade History, Analysis Viewer
- `onerror`-Fallback wenn kein Icon vorhanden

### Logs-Frontend
- Log-ID `#NNN` wird vor jedem Timestamp angezeigt
- Gemini-Channel-Filter im Dropdown ergänzt
- Gemini-Einträge in Teal-Farbe
- Context-Expand-Offset an neue ID-Spalte angepasst

### Sell-Order-Fix (KRITISCH)
- **Problem:** SELL-Orders liefen über `SOL-USD` → `account is not available` (EUR-Konto)
- **Fix 1:** Auf `SOL-EUR` umgestellt — aber 501-Fehler beim Product-Endpoint
- **Fix 2:** `getProductInfo()` (USD-Pair) für base_increment verwendet → `0.00000001` (8 Stellen) statt `0.001` (3 Stellen) → `INVALID_SIZE_PRECISION`
- **Fix 3 (final):** EUR-Product-Endpoint direkt nutzen für Preis UND base_increment — funktioniert korrekt
- Alle 26 EUR-Pairs geprüft und online: base_increment variiert stark (SHIB/ALGO/XLM = 0 Dezimalstellen, BTC/ETH = 8)

### Test-Trade-Button
- Neuer Button "Test Trade" in der Decisions-Tabelle (SOL SELL €1 mit Bestätigungsdialog)
- `createTestTrade()` in `Dashboard.php` — erstellt Analysis + TradeDecision
- Spinner beim Execute-Button via `wire:loading`

### scraper:score-pending Subagent
- 816 Artikel via Gemini gescort (0 Fehler, 0 ausstehend)
- Gemini Free-Tier-Quota erschöpft, reset ~09:00 MEZ

### CLAUDE.md
- Critical Rule: Alle 26 tradeable Assets berücksichtigen, NICHT nur BTC/ETH/SOL/XRP

## Wichtige Erkenntnisse
- Coinbase EUR-Konto: ALLE Orders (BUY + SELL) müssen über `ASSET-EUR` Pairs laufen
- `base_increment` muss vom EUR-Pair gelesen werden — USD-Pair hat andere Werte
- Gemini Free-Tier: ~800 Scoring-Calls/Tag reichen nicht für initiales Bulk-Import
