# Session 2026-03-23

## Änderungen

### Error Fixes – proposed_solution
- Neue Spalte `proposed_solution` (TEXT, nullable) in `error_fixes`
- Claude-Prompt in `ErrorFixService::analyzeWithClaude()` erweitert: gibt jetzt konkrete Schritt-für-Schritt-Lösung zurück (Dateipfade, Methodennamen, Config-Keys, max. 600 Zeichen)
- 72 bestehende Einträge rückwirkend befüllt (aus `fix_type` + `fix_command` abgeleitet)
- View zeigt proposed_solution als eigene Box zwischen Beschreibung und Command

### Error Fixes – Apply Fix with Claude
- Neuer Button "Apply Fix with Claude" für `fix_type = 'code'` + `!fix_applied`
- `ErrorFixService::applyCodeFixWithClaude(ErrorFix $fix)`:
  - Extrahiert relevante PHP-Dateipfade aus Error-Message und proposed_solution
  - Schickt Error + proposed_solution + Datei-Inhalte an Claude CLI
  - Claude antwortet mit `[{file, search, replace}]` (max. 3 Patches)
  - Validierung: Datei muss existieren, `search` muss exakt im File vorkommen
  - Wendet Patches per `str_replace` an, updated `fix_applied` + `fix_result`
- Livewire `ErrorFixes::applyFix(int $id)` mit Spinner während Claude arbeitet
- Result-Anzeige: grüne Box bei Erfolg, gelbe Box bei Fehler/Skip

### CSS
- Neue Klasse `.btn-neon-blue` (analog zu btn-neon-green/red)

### Error Fixes – ID sichtbar
- `#{{ $fix->id }}` als grauer Mono-Text vor dem Type-Badge

### News Sources
- Defekte Sources entfernt: Ripple Insights (404), alle Reddit-Feeds (403)
- Neue funktionierende Sources hinzugefügt:
  - Solana Foundation Blog (weight 1.8, official)
  - Ethereum Foundation Blog (weight 1.8, official)
  - CryptoSlate, U.Today, AMBCrypto (weight 1.3, news)
  - NewsBTC, Daily Hodl, CryptoPotato (weight 1.2, news)
  - ZyCrypto (weight 1.1, news)
- Ziel: bessere SOL/XRP/Altcoin-Abdeckung in der Analyse

### Analyse-Asset-Scope (rückgängig gemacht)
- Versuch: Signal-Generierung auf `allowed_assets` beschränken → rückgängig
- Korrekte Logik: Signals für alle Assets sammeln, nur Entscheidungen auf allowed_assets beschränken
- Signal-Prompt behält die breite Asset-Liste (BTC, ETH, SOL, XRP, DOGE, ADA, XLM etc.)

## Commits
- `8936daf` Feature: proposed_solution, Apply Fix with Claude, btn-neon-blue

## Offene Punkte
- Reddit-RSS-Feeds funktionieren nicht (403) — alternative Social-Signal-Quelle fehlt noch für XRP
- Ripple hat keinen öffentlichen RSS-Feed
